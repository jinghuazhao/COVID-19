#!/usr/bin/bash

export chr=chr${SLURM_ARRAY_TASK_ID}
${STEP2} step2 -c ${COHORT} -p work/${weswgs}/${pheno} -y work/${weswgs}-${chr}.gds \
               -m work/${weswgs} -t GCTA -o work/${weswgs}/${COHORT}-${weswgs}-${pheno}-${chr}

